# Пошаговый гайд: как выставить Mini App и кнопку в боте

Всё по шагам, без лишних слов. Следуй по порядку.

---

## Что должно быть заранее

- Репозиторий с этим проектом на **GitHub** (уже залит или склонирован).
- Твой логин на GitHub — запомни, он понадобится (например `mtvej` или `nertexqq`).
- Название репозитория — тоже запомни (например `portals_gifts_bot` или `miniapp-pars`).

---

## Ноль. Только что создал пустой репо на GitHub — что делать?

Если ты создал **новый пустой репозиторий** (как на скриншоте с «Quick setup») и проект у тебя уже лежит на компе в папке, делай так:

1. Открой терминал (PowerShell или cmd) и перейди в папку проекта:
   ```bash
   cd путь\к\папке\portals_gifts_bot
   ```
2. Если тут ещё нет git — инициализируй и сделай первый коммит:
   ```bash
   git init
   git add .
   git commit -m "first commit"
   git branch -M main
   ```
3. Привяжи свой репозиторий с GitHub (подставь свой логин и название репо; пример с твоего скрина):
   ```bash
   git remote add origin https://github.com/nertexqq/miniapp-pars.git
   ```
4. Отправь код на GitHub:
   ```bash
   git push -u origin main
   ```
   Введи логин и пароль (или токен) GitHub, если спросит.

После этого репозиторий на GitHub будет заполнен. Дальше — **Часть 1** (включить Pages и задеплоить Mini App).  
Адрес Mini App будет: `https://nertexqq.github.io/miniapp-pars/` (если логин `nertexqq`, репо `miniapp-pars`).

**Если Git просит указать, кто ты (`Author identity unknown`):**

Перед первым коммитом один раз настрой имя и почту (подставь свои данные; почта может быть та же, что на GitHub):

```bash
git config --global user.email "твоя@почта.com"
git config --global user.name "Твоё Имя"
```

Можно указать ник с GitHub: `git config --global user.name "nertexqq"`.  
После этого снова выполни `git add .` и `git commit -m "first commit"`.

**Если при `git push -u origin main` вылезла ошибка `src refspec main does not match any`:**

- Скорее всего, в репо ещё нет ни одного коммита (ветка `main` не создалась). Сделай так:
  1. Добавь все файлы и сделай первый коммит:
     ```bash
     git add .
     git commit -m "first commit"
     ```
  2. Назови ветку `main` (если она сейчас `master` или без имени):
     ```bash
     git branch -M main
     ```
  3. Пуш:
     ```bash
     git push -u origin main
     ```
- Если ветка у тебя называется **master**, можно не переименовывать, а пушить так:
  ```bash
  git push -u origin master
  ```
  Тогда на GitHub будет ветка `master`; workflow деплоя у нас срабатывает и на `main`, и на `master`, так что всё ок.

---

## Часть 1. Выложить Mini App в интернет (GitHub Pages)

### Шаг 1.1. Включить GitHub Pages

1. Открой свой репозиторий на GitHub.
2. Нажми вверху вкладку **Settings** (Настройки).
3. Слева в меню найди раздел **Pages** (в блоке "Code and automation").
4. В разделе **Build and deployment**:
   - в поле **Source** выбери **GitHub Actions** (не Branch!).
5. Ничего больше там настраивать не нужно — просто запомни, что Pages включены.

### Шаг 1.2. Запустить выкладку (деплой)

**Вариант А — через пуш в репозиторий**

- Сделай любой коммит и запушь в ветку **main** (или **master**).  
  Например: измени что-то в `README.md`, закоммить и выполнить `git push`.  
  Через 1–2 минуты Mini App автоматически выложится.

**Вариант Б — вручную из вкладки Actions**

1. В репозитории открой вкладку **Actions**.
2. Слева выбери workflow **Deploy Mini App to GitHub Pages**.
3. Справа нажми **Run workflow** → снова **Run workflow**.
4. Подожди, пока зелёная галочка появится у последнего запуска (обычно 1–2 минуты).

### Шаг 1.3. Узнать адрес своего Mini App

Адрес всегда такой:

```
https://ТВОЙ_ЛОГИН.github.io/НАЗВАНИЕ_РЕПО/
```

**Примеры:**

- Логин `mtvej`, репо `portals_gifts_bot` →  
  `https://mtvej.github.io/portals_gifts_bot/`
- Логин `ivan`, репо `portals_gifts_bot` →  
  `https://ivan.github.io/portals_gifts_bot/`

Проверка: открой этот адрес в браузере — должна открыться страница «Подарки» с кнопкой «Мониторинг». Если видишь её — Mini App выложен.

**Важно:** для бота нужен этот же URL, но **без слэша в конце**.  
Правильно: `https://mtvej.github.io/portals_gifts_bot`  
Не надо: `https://mtvej.github.io/portals_gifts_bot/`

---

## Часть 2. Подключить Mini App к боту (кнопка в меню)

### Шаг 2.1. Открыть файл с настройками бота

В папке проекта найди файл **`.env`**.  
Если его нет — скопируй **`.env.example`** и переименуй копию в **`.env`**.

### Шаг 2.2. Добавить одну строку

Открой `.env` в любом редакторе и добавь (или измени) строку:

```env
MINIAPP_URL=https://ТВОЙ_ЛОГИН.github.io/НАЗВАНИЕ_РЕПО
```

**Пример:**

```env
MINIAPP_URL=https://mtvej.github.io/portals_gifts_bot
```

- Без слэша в конце.
- Без пробелов вокруг `=`.
- Логин и название репо — свои, как в шаге 1.3.

Остальные переменные в `.env` (BOT_TOKEN, API_ID, DB_HOST и т.д.) должны быть уже заполнены, как для обычного запуска бота.

**Если API у тебя на отдельном сервере** и хочешь, чтобы по кнопке сразу открывался Mini App с этим API, можно указать полный URL с параметром:
```env
MINIAPP_URL=https://ТВОЙ_ЛОГИН.github.io/НАЗВАНИЕ_РЕПО?api=https://адрес-твоего-api.com
```
(без слэша в конце, `?api=...` — в конце одной строки).

### Шаг 2.3. Перезапустить бота

Останови бота (Ctrl+C, если запускал вручную) и запусти снова:

```bash
python -m src.bot.main
```

### Шаг 2.4. Проверить в Telegram

1. Открой своего бота в Telegram.
2. Нажми **Start** или отправь `/start`.
3. В меню должна появиться кнопка **«📱 Мониторинг подарков»**.
4. Нажми её — откроется окно с интерфейсом мониторинга (Mini App).

Если кнопки нет — проверь, что в `.env` точно указан `MINIAPP_URL` без опечаток и без слэша в конце, и что бот перезапущен после изменения `.env`.

---

## Часть 3. Чтобы в Mini App реально приходили подарки (API)

Сам по себе Mini App — это только интерфейс. Список подарков и мониторинг отдаёт **бэкенд** (GUI-сервер из папки `gui/`). Есть два варианта.

### Вариант А. Всё на своём компьютере (для теста)

1. Запусти GUI-сервер в той же папке проекта:
   ```bash
   python gui/server.py
   ```
   Должно появиться что-то вроде: `Running on http://127.0.0.1:5000`.

2. Mini App у тебя открывается с GitHub Pages, а API — на твоём компе. Чтобы они связались, нужен временный публичный адрес. Например, через **ngrok**:
   - Установи ngrok, запусти: `ngrok http 5000`.
   - Скопируй выданный HTTPS-адрес (типа `https://abc123.ngrok.io`).

3. В боте кнопка должна открывать Mini App **с параметром** `api`. То есть в `.env` задаёшь не просто URL Mini App, а URL с `?api=...`:
   - Так нельзя прописать в `.env` одной строкой, поэтому проще один раз открыть Mini App по ссылке вручную:
   ```
   https://ТВОЙ_ЛОГИН.github.io/НАЗВАНИЕ_РЕПО/?api=https://ТВОЙ_АДРЕС_NGROK
   ```
   (подставь свой логин, репо и адрес ngrok без слэша в конце).

   Либо в BotFather: **Bot Settings → Menu Button → Configure menu button** и вставить эту длинную ссылку с `?api=...`. Тогда по кнопке меню будет открываться уже «привязанный» к твоему API Mini App.

После этого при включённом мониторинге в Mini App начнут появляться подарки.

### Вариант Б. API на сервере (нормальный вариант)

1. Размести проект на сервере (VPS, облако и т.п.).
2. Запусти там бота и GUI-сервер (`python gui/server.py`) так, чтобы к нему был доступ по HTTPS (например через nginx + SSL).
3. Пусть адрес API будет, например, `https://gifts-api.mysite.com`.
4. В BotFather в **Menu Button** укажи:
   ```
   https://ТВОЙ_ЛОГИН.github.io/НАЗВАНИЕ_РЕПО/?api=https://gifts-api.mysite.com
   ```
   Тогда кнопка в боте будет открывать Mini App уже с этим API, и подарки будут подгружаться с твоего сервера.

---

## Краткая шпаргалка

| Что сделать | Где / как |
|-------------|-----------|
| Включить выкладку Mini App | GitHub → репо → **Settings → Pages → Source: GitHub Actions** |
| Выложить Mini App | **Actions → Deploy Mini App to GitHub Pages → Run workflow** (или пуш в main) |
| Узнать URL Mini App | `https://ТВОЙ_ЛОГИН.github.io/НАЗВАНИЕ_РЕПО/` |
| Показать кнопку в боте | В `.env` добавить `MINIAPP_URL=https://...` (без слэша в конце), перезапустить бота |
| Чтобы в приложении были подарки | Поднять GUI (`python gui/server.py`), дать ему HTTPS-адрес и открывать Mini App с `?api=https://...` (или прописать этот URL в Menu Button в BotFather) |

Если что-то из шагов не получается — напиши, на каком шаге застрял и что именно видишь на экране (или какую ошибку выдаёт).
