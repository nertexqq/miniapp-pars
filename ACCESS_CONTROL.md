# Система контроля доступа

## Описание

Бот использует строгую систему контроля доступа: **только пользователи, добавленные администратором, могут использовать бота**.

## Как это работает

1. **Middleware AccessControlMiddleware** проверяет каждого пользователя перед обработкой любого запроса
2. Если пользователь не в списке разрешенных (`allowed_users`) и не является админом - запрос блокируется
3. Пользователь получает сообщение: "❌ У вас нет доступа к этому боту. Обратитесь к администратору для получения доступа."

## Добавление пользователей

Только администратор может добавлять пользователей:

1. Откройте админ-панель (кнопка "👑 Админ-панель" в главном меню)
2. Нажмите "➕ Добавить пользователя"
3. Введите Telegram ID пользователя
4. Пользователь будет добавлен в таблицу `allowed_users`

## Удаление пользователей

1. Откройте админ-панель
2. Нажмите "➖ Удалить пользователя"
3. Введите Telegram ID пользователя
4. Пользователь будет удален из таблицы `allowed_users`

## Структура БД

```sql
-- Таблица администраторов
CREATE TABLE admins (
    user_id BIGINT PRIMARY KEY,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица разрешенных пользователей
CREATE TABLE allowed_users (
    user_id BIGINT PRIMARY KEY,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## Проверка доступа

Функция `is_allowed()` проверяет:
1. Является ли пользователь админом? → Доступ разрешен
2. Есть ли пользователь в `allowed_users`? → Доступ разрешен
3. Иначе → Доступ запрещен

## Важно

- **Администратор** (из таблицы `admins`) имеет полный доступ автоматически
- **Обычные пользователи** должны быть добавлены администратором в `allowed_users`
- Без добавления в `allowed_users` пользователь **не может делать ничего** в боте


