# Core
aiogram==3.11.0
aiomysql==0.2.0
# Pydantic (опционально, есть fallback)
# pydantic>=2.0.0
# pydantic-settings>=2.0.0

# Marketplaces (aportalsmp — async Portals, tonnelmp — Tonnel, MRKT через Pyrogram+api.tgmrkt.io)
aportalsmp>=1.3.0
pyrogram>=2.0.0
tonnelmp>=1.2.0
curl_cffi>=0.5.0

# Utils
python-dotenv==1.0.0
requests==2.31.0

# Optional: Redis for caching
redis[hiredis]>=5.0.0

# Optional: Webhook support
aiohttp>=3.9.0

# GUI (веб-интерфейс на Flask)
flask>=3.0.0
flask-socketio>=5.3.0
eventlet>=0.33.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
mypy>=1.7.0
